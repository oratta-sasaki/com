#pragma once

#include "frame/settings.h"

class MenuButtonLayout :public Menu{
public:
	MenuButtonLayout();
	~MenuButtonLayout();

	// 上下左右に合わせて配置する、という形にしたい
	// 表示位置：上下、左右、縦横並び、表示非表示アクション({Visible,Invisible})
//	MenuButtonLayout* create();

//	void addButtons(vector<MenuItem*> buttons);

protected:



private:




};

